version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.0
  build:
    commands:
      - echo Build started on `date`
      - composer install

artifacts:
  type: zip
  files:
    - '**/*'
